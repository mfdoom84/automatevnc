# =============================================================================
# AutoVNC Environment Configuration
# =============================================================================
# Copy this file to .env and modify the values as needed
# 
# Quick Start:
#   1. cp .env.example .env
#   2. docker compose up -d
#   3. Visit http://localhost:3000 for the web UI
#   4. Visit http://localhost:8080/docs for API documentation
# 
# Port Flexibility:
#   - Change FRONTEND_PORT and BACKEND_PORT to avoid conflicts
#   - CORS_ORIGINS will automatically update with the frontend port
#   - Works with any available ports on your system
# =============================================================================

# -----------------------------------------------------------------------------
# Port Configuration (Customizable)
# -----------------------------------------------------------------------------
# Frontend port - where the web UI will be accessible
# Default: 3000. Change if port 3000 is already in use.
FRONTEND_PORT=3000

# Host Bind Address - 127.0.0.1 for local only, 0.0.0.0 for LAN access
HOST_BIND_ADDRESS=127.0.0.1

# Backend port - where the API server and docs will be accessible  
# Default: 8080. Change if port 8080 is already in use.
BACKEND_PORT=8080

# CORS Origins - comma-separated list of allowed frontend URLs
# Automatically configured based on FRONTEND_PORT
# No need to change this unless you have special requirements
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# -----------------------------------------------------------------------------
# AI Provider Configuration (BYOK - Bring Your Own Key)
# -----------------------------------------------------------------------------
# Choose your AI provider: "openai" or "github"
AI_PROVIDER=openai

# OpenAI API Key (if using OpenAI)
OPENAI_API_KEY=

# GitHub Models API Key (if using GitHub Models)
GITHUB_MODELS_API_KEY=

# -----------------------------------------------------------------------------
# Optional: Default VNC Settings
# -----------------------------------------------------------------------------
# DEFAULT_VNC_HOST=
# DEFAULT_VNC_PORT=5900
